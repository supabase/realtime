Holds an assortment of dev related directories so that they're not all top-level.

* `bench` - benchmarks
* `postgres` - some scripts to help us get our local postgres in line with a hosted supabase postgres
* `scripts` - assorted scripts to run (such as a loadtesting script)