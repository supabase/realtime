# fly.toml file generated for realtime-multi on 2022-04-14T15:47:49+02:00

app = "realtime-multi"

kill_signal = "SIGTERM"
kill_timeout = 5

[deploy]
  release_command = "/app/bin/realtime eval Realtime.Release.migrate"
  strategy = "canary"

[env]
  DNS_NODES = "realtime-multi.internal"
  SECURE_CHANNELS = "true"

[experimental]
  private_network = true

[[services]]
  internal_port = 4000
  protocol = "tcp"

  [services.concurrency]
    hard_limit = 100000
    soft_limit = 100000

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [[services.tcp_checks]]
    grace_period = "30s"
    interval = "15s"
    restart_limit = 6
    timeout = "2s"
