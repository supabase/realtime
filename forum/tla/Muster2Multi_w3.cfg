\* Non-vacuity witness W3: expect VIOLATION of NoLaggingReadyRouter, proving the
\* Finding-A shape (a lagging :ready self-router behind a more-advanced peer) is
\* reachable in the multi-group model -- so NoMissedDelivery holding is not vacuous.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 2
  RingRank <- RingDiverge3
INVARIANT NoLaggingReadyRouter
