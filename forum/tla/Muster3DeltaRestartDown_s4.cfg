\* Sweep x delta x restart, delta-covering run: MaxSeq=4 (the add-only delta
\* path is only reachable at MaxSeq>=4, via a >=3-node shrink). Partial run;
\* MsgBound keeps the frontier tractable for a deeper reach.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 4
  RingRank <- RingDiverge3
CONSTRAINT MsgBound
INVARIANT NoMissedDelivery
