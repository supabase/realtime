\* Non-vacuity witness: expect VIOLATION of RestartRecoversToRouter, proving a
\* node that HAS restarted recovers into a Ready multi-node router actually
\* delivering to a live remote holder -- so the safety pass genuinely exercises
\* restart recovery and is not vacuous.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 3
  RingRank <- RingDiverge3
INVARIANT RestartRecoversToRouter
