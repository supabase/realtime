\* Non-vacuity probe: a DELTA snapshot in flight to a router that has already
\* swept rows (the add-only apply meets a sweep-edited table). Delta needs
\* MaxSeq>=4. NOT surfaced within a multi-minute search (BFS depth 12, 46.4M
\* distinct) -- kept as a documented probe, not a confirmed witness. The
\* confirmed delta witness is _w3 (NoDeltaSent, violated at depth 10); the
\* confirmed sweep witness is _w1 (MaxSeq=3, depth 7).
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 4
  RingRank <- RingDiverge3
CONSTRAINT MsgBound
INVARIANT NoDeltaToSweptRouter
