\* Non-vacuity probe: expect VIOLATED (a DELTA snapshot is dispatched at all in
\* this composition -- confirms the MaxSeq=4 safety run covers the add-only
\* path; mirrors Muster2Delta's NoDeltaSent probe). Delta needs MaxSeq>=4.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 4
  RingRank <- RingDiverge3
CONSTRAINT MsgBound
INVARIANT NoDeltaSent
