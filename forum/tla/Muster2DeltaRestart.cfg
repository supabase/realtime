\* Muster2Delta (multi-group + delta/full selection) COMPOSED with the coordinator
\* restart action. Checks NoMissedDelivery still holds when a restart (wiped
\* member_views / owed_snapshots / applied_snapshot_seq, surviving occ table, ring
\* reset to {n}, :converging singleton) coexists with the add-only delta path.
\* MaxSeq=2 is EXHAUSTIVE; note the delta path itself is only reachable at
\* MaxSeq>=4 (see Muster2Delta's structural finding), so this run is the
\* restart x multi-group x FULL-snapshot exhaustive baseline. The delta-covering
\* run is Muster2DeltaRestart_s4.cfg.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 2
  RingRank <- RingDiverge3
INVARIANT TypeOK
INVARIANT NoMissedDelivery
