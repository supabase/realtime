\* Sweep x restart at MaxSeq=4 -- the depth where Finding B lived (the restart
\* artifact violated at seq 4 / BFS depth 12 in Muster2Restart_s4). Partial run;
\* a clean pass past that depth corroborates the MaxSeq=2 exhaustive baseline.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  MaxSeq = 4
INVARIANT NoMissedDelivery
