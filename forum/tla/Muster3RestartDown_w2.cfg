\* Non-vacuity probe: expect VIOLATED (a sweep fires after a coordinator restart
\* has happened, i.e. the sweep judges post-restart agreement state).
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  MaxSeq = 2
INVARIANT NoSweepAfterRestart
