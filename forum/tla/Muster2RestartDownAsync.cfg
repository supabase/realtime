\* EXPECT VIOLATED: the async :DOWN window (Finding B residual) is real -- a
\* peer that was Ready before the restart stays Ready until it processes the
\* :DOWN, while the restarted node re-homes the group under its reset view.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  MaxSeq = 4
INVARIANT NoMissedDelivery
