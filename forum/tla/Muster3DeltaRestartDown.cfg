\* Sweep x delta/multi-group x restart (caveat 7, composition #2), exhaustive
\* baseline. Note the delta path itself is only reachable at MaxSeq>=4 (see
\* Muster2Delta's structural finding), so this run is the sweep x multi-group x
\* FULL-snapshot x restart exhaustive baseline; the delta-covering run is
\* Muster3DeltaRestartDown_s4.cfg.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 2
  RingRank <- RingDiverge3
INVARIANT TypeOK
INVARIANT NoMissedDelivery
