\* Restart x delta, delta-covering run: MaxSeq=4 (the delta path is only reachable
\* at MaxSeq>=4). Partial (state space grows past the heap); a clean partial run
\* corroborates the MaxSeq=2 exhaustive baseline. MsgBound keeps the frontier
\* tractable for a deeper reach into the concurrent-round / post-restart region.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  Groups = {"a", "b"}
  MaxSeq = 4
  RingRank <- RingDiverge3
CONSTRAINT MsgBound
INVARIANT TypeOK
INVARIANT NoMissedDelivery
