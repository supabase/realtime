\* REFUTED candidate (expect VIOLATED): "every miss is covered by a still-QUEUED
\* :DOWN". The witness is real: a dying incarnation the router never registered
\* gets no :DOWN at all, and its late marker re-asserts agreement behind a
\* withheld discover-ack piggyback. See the module comments and TLA_FINDINGS.md.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  MaxSeq = 4
INVARIANT MissImpliesPendingDown
