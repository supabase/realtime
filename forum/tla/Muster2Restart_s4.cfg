\* Cross-check: single-group restart model at MaxSeq=4 (the depth the composed
\* Muster2DeltaRestart violation lives at). Muster2Restart was only ever exhaustive
\* at MaxSeq=2 and partial at MaxSeq=3, so it never reached seq=4 states.
SPECIFICATION Spec
CONSTANTS
  Nodes = {1, 2, 3}
  MaxSeq = 4
INVARIANT NoMissedDelivery
